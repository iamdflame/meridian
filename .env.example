# Cleanverse credentials — arrive by automated email on hackathon registration.
# NEVER commit real values. The App Key is a LOCAL encryption key; it is never transmitted.
CLEANVERSE_API_ID=
CLEANVERSE_APP_KEY=
CLEANVERSE_COOPERATE_BASE=https://uatapi.cleanverse.com/api/cooperate
CLEANVERSE_SKILLS_BASE=https://uatapi.cleanverse.com/api/skills

# Monad testnet
MONAD_RPC=https://testnet-rpc.monad.xyz
MONAD_CHAIN_ID=10143
# Deployer / keeper key (testnet only, throwaway)
DEPLOYER_KEY=

# Meridian server
PORT=8787
# When 1, cooperate calls without credentials replay recorded fixtures and are tagged simulated:true
MERIDIAN_ALLOW_FIXTURES=1
